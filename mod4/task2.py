#Задача 2. 
#Возводить в степень можно гораздо быстрее, чем за n умножений! Для этого нужно воспользоваться следующими рекуррентными соотношениями:
#a^n = (a^2)^n/2  при четном n,
#a^n = a × a^n−1 при нечетном n.
#На вход подается два числа через пробел: a, n. Напишите функцию для реализации алгоритма быстрого возведения в степень с помощью рекурсивной функции.
def fast_power(a, n):
    # Базовый случай: любое число в степени 0 равно 1
    if n == 0:
        return 1
    
    # Если степень четная: (a^n) заменяем на ((a^2)^(n/2))
    # Это экономит количество умножений
    if n % 2 == 0:
        return fast_power(a * a, n // 2)
    
    # Если степень нечетная: отделяем одно основание 'a' 
    # и переходим к четной степени (n - 1)
    return a * fast_power(a, n - 1)

# Считываем два числа (основание и степень) через пробел
a, n = map(int, input().split())

# Вызываем функцию и выводим результат
print(fast_power(a, n))
